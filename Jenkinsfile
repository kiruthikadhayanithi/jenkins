pipeline {
    agent any

    stages {
        
        stage('Build') {
            steps {
                sh'npm install'

            }
        }
         stage('Example') {
            steps {
                sh'node --version'
                
            }
        } 
     }
 }
